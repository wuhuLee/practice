const localConfig = {
  mysql: {
    type: 'mysql',
    host: 'localhost',
    port: 3306,
    username: 'root',
    password: '13189392292',
    database: 'test',
  },
};

const config = localConfig;
export default config;
